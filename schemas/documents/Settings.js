const Settings = {
    title: 'Settings',
    name: 'settings',
    type: 'document',
    fields: [
        {
            title: 'Github action token',
            name: 'githubToken',
            type: 'string'
        }
    ]
};

export default Settings;
